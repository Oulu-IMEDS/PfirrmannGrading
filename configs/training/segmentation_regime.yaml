defaults:
  - loss_function: jaccard

learning_rate: 1e-4
epochs: 50

optimizer:
  weight_decay: 0.0001

scheduler:
  factor: 0.1
  patience: 5
  step_low: 25
  step_high: 50

dataloader:
  batch_size: 8
  num_workers: 1

train_test:
  test_perc: 0.2

checkpoint:
  epochs_to_pass: 10