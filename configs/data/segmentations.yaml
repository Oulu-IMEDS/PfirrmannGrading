segmentation:
  dir : /data/NFBC_Segmentation
  images: images
  masks: masks
  annotations: annotations.xml
  metadata: metadata.csv

dataloader:
  batch_size: 5
  num_workers: 1

split_ratio:
  train_split: 70
  val_split: 20
  test_split: 10